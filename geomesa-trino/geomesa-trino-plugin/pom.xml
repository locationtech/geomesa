<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.locationtech.geomesa</groupId>
        <artifactId>geomesa-trino_2.12</artifactId>
        <version>6.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>geomesa-trino-plugin_2.12</artifactId>
    <name>GeoMesa Trino Plugin</name>
    <packaging>trino-plugin</packaging>

    <properties>
        <skipITs>true</skipITs>
        <opentelemetry.version>1.65.0</opentelemetry.version> <!-- note: trino bundles 1.61.0, but that triggers a CVE in our scanning -->
        <!-- The geomesa parent pins jetty to 9.4 for its own server stack, but Trino 483's
             embedded test HTTP server (DistributedQueryRunner, used only by the end-to-end
             visibility-pruning IT) requires jetty 12 — 9.4 lacks UriCompliance.Violation and
             fails at server startup. Redirect the parent's jetty-bom import to the version Trino
             pulls. Safe here: this module's main (plugin) code has no jetty dependency, so this
             only realigns the test classpath. -->
        <jetty.version>12.1.11</jetty.version>
    </properties>

    <dependencyManagement>
        <dependencies>

            <dependency>
                <groupId>io.trino</groupId>
                <artifactId>trino-spi</artifactId>
                <version>${trino.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.locationtech.jts</groupId>
                <artifactId>jts-core</artifactId>
                <version>${jts.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>2.21</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>io.airlift</groupId>
                <artifactId>slice</artifactId>
                <version>2.7</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-api</artifactId>
                <version>${opentelemetry.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-api-incubator</artifactId>
                <version>${opentelemetry.version}-alpha</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-context</artifactId>
                <version>${opentelemetry.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-common</artifactId>
                <version>${opentelemetry.version}</version>
                <scope>provided</scope>
            </dependency>
            <!-- The geomesa parent pins jakarta.annotation-api to 1.3.5, which ships the
                 old javax.annotation namespace. -->
            <dependency>
                <groupId>jakarta.annotation</groupId>
                <artifactId>jakarta.annotation-api</artifactId>
                <version>3.0.0</version>
            </dependency>

            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.jupiter.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- The geomesa parent pins commons-math3 to 'provided' (license reasons). It is
                 pulled in here only transitively through the test-scoped trino-testing engine, so
                 override it to 'test': it must stay on the test classpath, and the trino-plugin
                 packaging check rejects any non-SPI 'provided' dependency. -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-math3</artifactId>
                <version>3.6.1</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.apache.iceberg</groupId>
            <artifactId>iceberg-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.iceberg</groupId>
            <artifactId>iceberg-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.locationtech.jts</groupId>
            <artifactId>jts-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.locationtech.geomesa</groupId>
            <artifactId>geomesa-z3_${scala.binary.version}</artifactId>
        </dependency>

        <!-- trino provided dependencies -->
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-spi</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>
        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>slice</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api</artifactId>
        </dependency>

        <!-- The geomesa parent manages jsr305 to 'provided' (geomesa's own runtime
             supplies it), but the Trino runtime does not, so force it back to compile so
             it is bundled. The trino-maven-plugin rejects provided non-SPI dependencies. -->
        <dependency>
            <groupId>com.google.code.findbugs</groupId>
            <artifactId>jsr305</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- Trino iceberg connector — compile so we can wrap IcebergPlugin -->
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-iceberg</artifactId>
            <version>${trino.version}</version>
            <!-- Exclusions added to minimize the size of the shaded jar -->
            <exclusions>
                <!-- Snowflake catalog + its JDBC driver (~230 MB) -->
                <exclusion>
                    <groupId>net.snowflake</groupId>
                    <artifactId>snowflake-jdbc</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.iceberg</groupId>
                    <artifactId>iceberg-snowflake</artifactId>
                </exclusion>
                <!-- Azure filesystem/catalog -->
                <exclusion>
                    <groupId>io.trino</groupId>
                    <artifactId>trino-filesystem-azure</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.iceberg</groupId>
                    <artifactId>iceberg-azure</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.azure</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
                <!-- Google Cloud Storage filesystem/catalog (drops gax/grpc/google-cloud-* transitively) -->
                <exclusion>
                    <groupId>io.trino</groupId>
                    <artifactId>trino-filesystem-gcs</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.iceberg</groupId>
                    <artifactId>iceberg-gcp</artifactId>
                </exclusion>
                <!-- Alluxio filesystem + cache -->
                <exclusion>
                    <groupId>io.trino</groupId>
                    <artifactId>trino-filesystem-alluxio</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>io.trino</groupId>
                    <artifactId>trino-filesystem-cache-alluxio</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-plugin-toolkit</artifactId>
            <version>${trino.version}</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-jdk14</artifactId>
            <version>${slf4j.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-geospatial-toolkit</artifactId>
            <version>${trino.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.accumulo</groupId>
            <artifactId>accumulo-access</artifactId>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-jdbc</artifactId>
            <version>${trino.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Full Trino query engine (DistributedQueryRunner + TESTING_FILE_METASTORE iceberg
             catalog) for the tier-2 end-to-end visibility-pruning IT. Test scope only: it is
             not bundled into the trino-plugin shaded jar. Heavy to spin up, so the IT is a
             *IT class (Failsafe, -DskipITs=false) and tagged @Tag("integration") so the fast
             surefire lane skips it (see this module's excludedGroups=integration). -->
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-testing</artifactId>
            <version>${trino.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Hadoop filesystem support (io.trino.filesystem.hdfs.HdfsFileSystemManager), required on
             the test classpath when the in-process iceberg catalog runs with fs.hadoop.enabled=true
             — Hadoop's LocalFileSystem serves the file:// warehouse the IT writes to. Test scope. -->
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-hdfs</artifactId>
            <version>${trino.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- We use the maven compiler plugin instead of the scala plugin as it doesn't support java 25 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${jdk.version}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>net.alchim31.maven</groupId>
                <artifactId>scala-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>scala-sources</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>scala-versioned-sources</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>scala-compile-first</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>scala-test-compile</id>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>io.trino</groupId>
                <artifactId>trino-maven-plugin</artifactId>
                <version>24</version>
                <extensions>true</extensions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <!-- this module uses JUnit 5 (Jupiter), which requires the surefire-junit-platform
                     provider. Elsewhere in the build surefire auto-detects the JUnit 4 provider
                     for specs2 (which reports its example names correctly under JUnit 4); here we
                     configure the platform provider explicitly since only JUnit 5 is on the classpath. -->
                <dependencies>
                    <dependency>
                        <groupId>org.apache.maven.surefire</groupId>
                        <artifactId>surefire-junit-platform</artifactId>
                        <version>${maven.surefire.plugin.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
            <!-- Declared active here (not just pluginManagement, which doesn't bind executions)
                 so both child modules run their *IT classes under -DskipITs=false without
                 re-declaring the plugin themselves. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <!-- The end-to-end IT forks a real Trino engine (DistributedQueryRunner), which
                         requires the same JVM options Trino's own tests run with: the incubator
                         Vector module (BlockEncodingSimdSupport fails to initialize without it) and,
                         on JDK 24+, explicit sun.misc.Unsafe memory-access. Mirrors Trino's
                         air.test.jvm.additional-arguments. Only failsafe (the IT) needs these. -->
                    <argLine>--add-modules=jdk.incubator.vector --sun-misc-unsafe-memory-access=allow -XX:+UnlockDiagnosticVMOptions</argLine>
                </configuration>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <excludedGroups>integration</excludedGroups>
                        <jdkToolchain>
                            <version>${jdk.version}</version>
                        </jdkToolchain>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <executions>
                        <execution>
                            <goals>
                                <goal>integration-test</goal>
                                <goal>verify</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <skipITs>${skipITs}</skipITs>
                        <jdkToolchain>
                            <version>${jdk.version}</version>
                        </jdkToolchain>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <!-- This module is java-only, so the scala doc tooling the geomesa
                         parent uses doesn't apply: attach javadocs with the regular javadoc plugin
                         (required for maven central) -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <!-- populates ${dep.cp}, which the geomesa parent's javadoc config puts on
                             the javadoc class path -->
                        <executions>
                            <execution>
                                <id>generate-classpath</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>build-classpath</goal>
                                </goals>
                                <configuration>
                                    <outputFile>${project.build.directory}/module-classpath</outputFile>
                                    <outputProperty>dep.cp</outputProperty>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
