# <0.12: the nested-metrics patch in common.py is ported from 0.11.1 —
# re-verify before raising the bound.
pyiceberg[s3fs,pyarrow]>=0.11.1,<0.12
trino>=0.328.0
shapely>=2.0.0
pyarrow>=15.0.0
boto3>=1.34.0
pytest>=8.0.0
